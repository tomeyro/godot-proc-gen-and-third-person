[gd_resource type="ShaderMaterial" load_steps=2 format=2]

[sub_resource type="Shader" id=1]
resource_local_to_scene = true
code = "shader_type spatial;

uniform float move = 0.5;
uniform vec4 color : hint_color = vec4(1.0);
uniform vec3 object_translation = vec3(0.0);
uniform float distance_from_center = 0.0;

void vertex() {
    VERTEX.y += abs(sin(TIME + VERTEX.x + VERTEX.z + object_translation.x + object_translation.z)) * move;
}

void fragment() {
    ALBEDO = color.rgb - (vec3(.5) * distance_from_center);
}"

[resource]
shader = SubResource( 1 )
shader_param/move = 0.5
shader_param/color = Color( 0.141176, 0.513726, 0.784314, 1 )
shader_param/object_translation = Vector3( 0, 0, 0 )
shader_param/distance_from_center = 0.0
